stages:
  - lint
  - test

variables:
  PYTHONPATH: "$CI_PROJECT_DIR"

lint:
  stage: lint
  script:
    - echo "Running lint step"
    - pip install -r requirements.txt
    - flake8 backend || true

test:
  stage: test
  script:
    - pip install -r requirements.txt
    - pytest -q --maxfail=1